'use strict';

/**
 * producto-agricola service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::producto-agricola.producto-agricola');
