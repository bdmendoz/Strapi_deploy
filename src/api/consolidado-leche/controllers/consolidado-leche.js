'use strict';

/**
 *  consolidado-leche controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::consolidado-leche.consolidado-leche');
