'use strict';

/**
 * consolidado-leche service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::consolidado-leche.consolidado-leche');
